
* Solve warnings about java classpath
* JVM doesn't run tests in dependent projects (including from top level), but JS does
* A module having no tests causes an error, which should either not happen or be more clear
* Gradle clean and run tasks don't work together (error about NPM and installKotlinJs)
*


